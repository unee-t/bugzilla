version: '2'
services:
    meteor:
      image: ulexus/meteor
      ports:
       - "8080:80"
      environment:
       - BUNDLE_URL=https://unee-t-media.s3-accelerate.amazonaws.com/2017-08-31/unee-t-fe.tar.gz
       - ROOT_URL=https://dev.unee-t.com
       - BUGZILLA_URL=https://bugzilla.dev.unee-t.com/
       - BUGZILLA_ADMIN_KEY=I6zRu7bPak687rcIBCkNbFKblfRXPn2X3xgEFz99
       - MONGO_URL=mongodb://mongo:27017/mydatabase
      logging:
            driver: awslogs
            options:
              awslogs-region: ap-southeast-1
              awslogs-group: meteor
      links:
          - "mongo"

    mongo:
      image: mongo
      volumes:
        - '/data/db:/data/db'
      ports:
      - "27017:27017"
      logging:
              driver: awslogs
              options:
                awslogs-region: ap-southeast-1
                awslogs-group: mongo

